const NoMoreJob = () => {
  return (
    <div class="text-center">
      <div class="text-xl font-medium">Sorry...no more job to show 🥲</div>
      <div>We are currently working on a way to display more job postings.</div>
    </div>
  );
};

export default NoMoreJob;
